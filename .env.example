# Environment Variables for my-calculator
# Copy this file to .env and fill in your values
# NEVER commit the real .env file

# JWT secret for signing auth tokens (generate with: openssl rand -hex 32)
JWT_SECRET=replace_with_your_generated_secret

# Add other environment variables below in KEY=VALUE format
# API_KEY=your_api_key_here

