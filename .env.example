# Environment Variables for My Calculator
# Copy this file to .env and fill in your actual values
# Example:
# API_KEY=your_api_key_here

# Used for signing auth tokens (JWT/session).
# Generate a new one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=replace_me_with_a_real_secret

