{"result":"     1\t# Architecture\n     2\t\n     3\tThis document explains the technical architecture of the **My Calculator** project. It is intended for developers who want to understand how the codebase is organized or who plan to extend it with new calculators and pages.\n     4\t\n     5\t---\n     6\t\n     7\t## Overview\n     8\t\n     9\tMy Calculator is a **multi-page static website**. Each calculator is a standalone HTML page with its own CSS and JavaScript. There is:\n    10\t\n    11\t- **No server** — all files are served as static assets.\n    12\t- **No bundler** — no Webpack, Rollup, Vite, or similar build step.\n    13\t- **No framework** — no React, Vue, Angular, or other UI framework.\n    14\t\n    15\tPages are linked to one another via plain `<a href>` tags. The project can be opened directly in a browser by loading `index.html`, or served from any static file server (e.g., `npx serve`, GitHub Pages, Netlify).\n    16\t\n    17\t---\n    18\t\n    19\t## Three-Layer Pattern\n    20\t\n    21\tEvery page in the project follows the same three-layer structure: HTML for structure, CSS for presentation, and JavaScript for behavior.\n    22\t\n    23\t### HTML\n    24\t\n    25\tEach page uses semantic HTML with `<section>` elements for content areas. At the top of `<body>`, every page includes a placeholder div where the shared header is injected:\n    26\t\n    27\t```html\n    28\t<body>\n    29\t  <div id=\"header-container\"></div>\n    30\t\n    31\t  <section>\n    32\t    <!-- Page-specific content -->\n    33\t  </section>\n    34\t\n    35\t  <!-- Stylesheets and scripts loaded at the bottom -->\n    36\t</body>\n    37\t```\n    38\t\n    39\tThe `<div id=\"header-container\"></div>` is the mount point for the shared header component (see [Shared Header Component](#shared-header-component) below).\n    40\t\n    41\t### CSS\n    42\t\n    43\tTwo stylesheets are loaded per page:\n    44\t\n    45\t1. **`assets/styles/app.css`** — shared global styles including CSS reset, header, calculator box, and buttons.\n    46\t2. **Page-specific stylesheet** (e.g., `home.css`, `xpy.css`) — styles unique to that page's layout and content.\n    47\t\n    48\t```html\n    49\t<link rel=\"stylesheet\" href=\"assets/styles/app.css\">\n    50\t<link rel=\"stylesheet\" href=\"assets/styles/home.css\">\n    51\t```\n    52\t\n    53\t> **Note:** The basic calculator subdirectory (`basics-10-function-refactoring/`) maintains its own copy of `app.css` so it can operate as a self-contained sub-application.\n    54\t\n    55\t### JavaScript\n    56\t\n    57\tTwo scripts are loaded per page, both with the `defer` attribute:\n    58\t\n    59\t1. **`components/Header.js`** — the shared header component (loaded first).\n    60\t2. **Page-specific script** (e.g., `home.js`, `xpy.js`) — loaded second; calls `renderHeader()` on `DOMContentLoaded`.\n    61\t\n    62\t```html\n    63\t<script defer src=\"assets/scripts/components/Header.js\"></script>\n    64\t<script defer src=\"assets/scripts/home.js\"></script>\n    65\t```\n    66\t\n    67\t---\n    68\t\n    69\t## Shared Header Component\n    70\t\n    71\tThe shared header is defined in `assets/scripts/components/Header.js`. It provides a consistent header (title + navigation) across all pages without duplicating HTML.\n    72\t\n    73\t### Exported Functions\n    74\t\n    75\tThe component exports three functions:\n    76\t\n    77\t| Function | Signature | Description |\n    78\t|---|---|---|\n    79\t| `createHeader` | `createHeader(title, navLinks)` | Creates and returns a `<header>` DOM element with an `<h1>` title and optional `<nav>` of links. Does not attach it to the document. |\n    80\t| `renderHeader` | `renderHeader(containerId, title, navLinks)` | Finds a container element by its ID and appends a `<header>` element containing an `<h1>` title and an optional `<nav>` of links. |\n    81\t| `replaceHeader` | `replaceHeader(title, navLinks)` | Replaces the existing header content with a new title and set of navigation links. |\n    82\t\n    83\t### Usage\n    84\t\n    85\tEach page's script calls `renderHeader` on `DOMContentLoaded`, passing a page-specific title and navigation array:\n    86\t\n    87\t```js\n    88\tdocument.addEventListener('DOMContentLoaded', function () {\n    89\t  if (typeof renderHeader === 'function') {\n    90\t    renderHeader('header-container', 'My Calculator', [\n    91\t      { label: 'Home', href: '../index.html' },\n    92\t      { label: 'Gallery', href: '../gallery.html' },\n    93\t      { label: 'XPY', href: '../xpy/xpy.html' }\n    94\t    ]);\n    95\t  }\n    96\t});\n    97\t```\n    98\t\n    99\t### Module Export\n   100\t\n   101\tThe component uses a CommonJS-compatible export guard so it can be required in Node.js (e.g., for testing) while still working as a plain browser script:\n   102\t\n   103\t```js\n   104\tif (typeof module !== 'undefined' && module.exports) {\n   105\t  module.exports = { createHeader, renderHeader, replaceHeader };\n   106\t}\n   107\t```\n   108\t\n   109\t### Duplicate Copy\n   110\t\n   111\tA duplicate copy of `Header.js` exists at:\n   112\t\n   113\t```\n   114\tbasics-10-function-refactoring/assets/scripts/components/Header.js\n   115\t```\n   116\t\n   117\tThis allows the basic calculator subdirectory to load the header component using a relative path without referencing the parent directory.\n   118\t\n   119\t---\n   120\t\n   121\t## Script Loading & Execution Order\n   122\t\n   123\tAll `<script>` tags in the project use the `defer` attribute:\n   124\t\n   125\t```html\n   126\t<script defer src=\"assets/scripts/components/Header.js\"></script>\n   127\t<script defer src=\"assets/scripts/home.js\"></script>\n   128\t```\n   129\t\n   130\tThe `defer` attribute guarantees that scripts execute **in document order** after the HTML has been fully parsed. This means:\n   131\t\n   132\t1. `Header.js` executes first, defining `renderHeader` (and `createHeader`, `replaceHeader`) on the global scope.\n   133\t2. The page-specific script executes second and can safely call `renderHeader`.\n   134\t\n   135\tAs an additional safety guard, each page script wraps its call in a type check:\n   136\t\n   137\t```js\n   138\tif (typeof renderHeader === 'function') {\n   139\t  renderHeader(/* ... */);\n   140\t}\n   141\t```\n   142\t\n   143\tThis prevents runtime errors if `Header.js` fails to load for any reason.\n   144\t\n   145\t---\n   146\t\n   147\t## Calculator Sub-Applications\n   148\t\n   149\t### Basic Calculator (`basics-10-function-refactoring/`)\n   150\t\n   151\tThe only calculator with actual arithmetic logic. It uses a `vendor.js` → `app.js` split:\n   152\t\n   153\t- **`vendor.js`** — holds DOM element references and the `outputResult()` helper function.\n   154\t- **`app.js`** — contains the calculation engine, a log system, and event listeners.\n   155\t\n   156\t**Calculation engine functions:**\n   157\t\n   158\t| Function | Operation |\n   159\t|---|---|\n   160\t| `calculation()` | Main dispatch function that routes to the correct operation |\n   161\t| `add()` | Addition (ADD) |\n   162\t| `subtract()` | Subtraction (SUBT) |\n   163\t| `multiplication()` | Multiplication (MULTI) |\n   164\t| `division()` | Division (DIVID) |\n   165\t\n   166\t**Log system:**\n   167\t\n   168\t- `writeLog()` — appends an entry to the log.\n   169\t- `logEntry` — array storing calculation history.\n   170\t\n   171\t**Supported operations:** ADD, SUBT, MULTI, DIVID.\n   172\t\n   173\t### XPY Calculator (`xpy/`)\n   174\t\n   175\tSelf-contained `xpy.js` with a single calculation function:\n   176\t\n   177\t```js\n   178\tfunction calculateXPY(base, exponent) {\n   179\t  return Math.pow(base, exponent);\n   180\t}\n   181\t```\n   182\t\n   183\tFeatures Calculate and Clear buttons, plus Enter-key support for submitting input.\n   184\t\n   185\t### UYT Calculator (`uyt/`)\n   186\t\n   187\tSelf-contained `uyt.js` with a single calculation function:\n   188\t\n   189\t```js\n   190\tfunction calculateUYT(u, y, t) {\n   191\t  if (t === 0) {\n   192\t    // Divide-by-zero protection\n   193\t    return 'Error: division by zero';\n   194\t  }\n   195\t  return (u * y) / t;\n   196\t}\n   197\t```\n   198\t\n   199\tComputes `(u * y) / t` with divide-by-zero protection.\n   200\t\n   201\t### Informational Pages (ASD, QWE, CVXZ, NNN, SDSSA)\n   202\t\n   203\tThese pages are currently **informational/content-only**. Their JavaScript files do not contain calculation logic — the scripts only call `renderHeader()` to display the shared header. They describe specialized calculators but do not yet implement calculation functions.\n   204\t\n   205\t---\n   206\t\n   207\t## Styling Conventions\n   208\t\n   209\t| Property | Value |\n   210\t|---|---|\n   211\t| Primary color | `#023d6d` (dark blue) |\n   212\t| Hover color | `#084f88` |\n   213\t| Card background | `#f0f4f8` |\n   214\t| Font | Roboto (Google Fonts) |\n   215\t| Border-radius | 5–10px |\n   216\t| Box shadows | Applied to headers and hover states |\n   217\t\n   218\t### Responsive Design\n   219\t\n   220\tThe gallery page uses CSS Grid with `auto-fill` for a responsive card layout:\n   221\t\n   222\t```css\n   223\t.gallery-grid {\n   224\t  display: grid;\n   225\t  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));\n   226\t  gap: 1rem;\n   227\t}\n   228\t```\n   229\t\n   230\tA media query at `max-width: 600px` adjusts the layout for mobile devices:\n   231\t\n   232\t```css\n   233\t@media (max-width: 600px) {\n   234\t  .gallery-grid {\n   235\t    grid-template-columns: 1fr;\n   236\t  }\n   237\t}\n   238\t```\n   239\t\n   240\t---\n   241\t\n   242\t## Extending the Project\n   243\t\n   244\t### Adding a New Calculator\n   245\t\n   246\tTo add a new calculator page (e.g., `newcalc`):\n   247\t\n   248\t1. **Create the HTML file** — `newcalc.html`:\n   249\t\n   250\t   ```html\n   251\t   <!DOCTYPE html>\n   252\t   <html lang=\"en\">\n   253\t   <head>\n   254\t     <meta charset=\"UTF-8\">\n   255\t     <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">\n   256\t     <title>New Calculator</title>\n   257\t     <link rel=\"stylesheet\" href=\"assets/styles/app.css\">\n   258\t     <link rel=\"stylesheet\" href=\"assets/styles/newcalc.css\">\n   259\t   </head>\n   260\t   <body>\n   261\t     <div id=\"header-container\"></div>\n   262\t\n   263\t     <section>\n   264\t       <!-- Calculator content -->\n   265\t     </section>\n   266\t\n   267\t     <script defer src=\"assets/scripts/components/Header.js\"></script>\n   268\t     <script defer src=\"assets/scripts/newcalc.js\"></script>\n   269\t   </body>\n   270\t   </html>\n   271\t   ```\n   272\t\n   273\t2. **Create the page-specific stylesheet** — `assets/styles/newcalc.css`.\n   274\t\n   275\t3. **Create the page-specific script** — `assets/scripts/newcalc.js`:\n   276\t\n   277\t   ```js\n   278\t   document.addEventListener('DOMContentLoaded', function () {\n   279\t     if (typeof renderHeader === 'function') {\n   280\t       renderHeader('header-container', 'New Calculator', [\n   281\t         { label: 'Home', href: 'index.html' },\n   282\t         { label: 'Gallery', href: 'gallery.html' }\n   283\t       ]);\n   284\t     }\n   285\t\n   286\t     // Calculator logic here\n   287\t   });\n   288\t   ```\n   289\t\n   290\t4. **Add a link card** to `gallery.html` so users can discover the new calculator.\n   291\t\n   292\t5. **Add a button** on `index.html` to provide quick access from the home page.\n   293\t\n   294\t### Adding a Navigation Link to All Pages\n   295\t\n   296\tTo add a new navigation link that appears in the header on every page:\n   297\t\n   298\t1. Update the `navLinks` array in **every** page script's `renderHeader()` call.\n   299\t2. Add the new link object (e.g., `{ label: 'New Calc', href: 'newcalc.html' }`) to each array.\n   300\t\n   301\tThere is no central configuration file for navigation — each page maintains its own nav array. This is a known limitation of the static, framework-free architecture.\n   302\t\n","status":"success"}
